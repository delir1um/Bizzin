<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Daily Business Insights</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333333 !important;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff !important;
        }
        .container {
            background: #ffffff !important;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            border: 1px solid #e5e7eb;
        }
        .header {
            background: linear-gradient(135deg, #EA7A57 0%, #f97316 100%);
            color: white;
            padding: 30px 40px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .header p {
            margin: 8px 0 0 0;
            opacity: 0.9;
            font-size: 16px;
        }
        .content {
            padding: 40px;
            background-color: #ffffff !important;
        }
        .section {
            margin-bottom: 40px;
            padding: 0;
        }
        .section:last-child {
            margin-bottom: 0;
        }
        .section-title {
            font-size: 20px;
            font-weight: 600;
            color: #1f2937 !important;
            margin: 0 0 16px 0;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .section-content {
            font-size: 16px;
            line-height: 1.7;
            color: #4b5563 !important;
            background-color: #ffffff !important;
        }
        .journal-prompt {
            background: #f0f9ff !important;
            border-left: 4px solid #0ea5e9;
            padding: 20px;
            border-radius: 0 8px 8px 0;
            font-style: italic;
            margin: 16px 0;
            color: #1f2937 !important;
        }
        .goals-summary {
            background: #f0fdf4 !important;
            border-left: 4px solid #22c55e;
            padding: 20px;
            border-radius: 0 8px 8px 0;
            color: #1f2937 !important;
        }
        .business-insights {
            background: #fefce8 !important;
            border-left: 4px solid #eab308;
            padding: 20px;
            border-radius: 0 8px 8px 0;
            color: #1f2937 !important;
        }
        .milestone-reminders {
            background: #fdf2f8 !important;
            border-left: 4px solid #ec4899;
            padding: 20px;
            border-radius: 0 8px 8px 0;
            color: #1f2937 !important;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #EA7A57 0%, #f97316 100%);
            color: white !important;
            padding: 16px 32px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            margin: 24px 0;
            text-align: center;
            transition: transform 0.2s;
        }
        .cta-button:hover {
            transform: translateY(-1px);
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin: 24px 0;
        }
        .stat-card {
            background: #f8fafc !important;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid #e2e8f0;
        }
        .stat-number {
            font-size: 24px;
            font-weight: 700;
            color: #EA7A57 !important;
            display: block;
        }
        .stat-label {
            font-size: 14px;
            color: #6b7280 !important;
            margin-top: 4px;
        }
        .footer {
            background: #f8fafc !important;
            padding: 30px 40px;
            text-align: center;
            border-top: 1px solid #e2e8f0;
            font-size: 14px;
            color: #6b7280 !important;
        }
        .footer a {
            color: #EA7A57 !important;
            text-decoration: none;
        }
        .sentiment-indicator {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
            margin: 0 8px;
        }
        .sentiment-positive {
            background: #dcfce7;
            color: #166534;
        }
        .sentiment-neutral {
            background: #f3f4f6;
            color: #374151;
        }
        .sentiment-negative {
            background: #fee2e2;
            color: #991b1b;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .content {
                padding: 20px;
            }
            .header {
                padding: 20px;
            }
            .stats-grid {
                grid-template-columns: 1fr 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>Good morning, {{personalization.userName}}! 🌟</h1>
            <p>Your daily business insights for {{personalization.currentDate}}</p>
        </div>

        <!-- Main Content -->
        <div class="content">
            <!-- Quick Stats -->
            <div class="stats-grid">
                <div class="stat-card">
                    <span class="stat-number">{{personalization.totalGoals}}</span>
                    <div class="stat-label">Active Goals</div>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{personalization.recentEntryCount}}</span>
                    <div class="stat-label">Journal Entries (7d)</div>
                </div>
            </div>

            <!-- Journal Prompt Section -->
            <div class="section">
                <h2 class="section-title">
                    ✍️ Today's Journal Prompt
                </h2>
                <div class="journal-prompt">
                    {{journal_prompt}}
                </div>
                <div style="text-align: center;">
                    <a href="{{baseUrl}}/journal/new?prompt={{journal_prompt}}" class="cta-button">
                        Write Your Entry
                    </a>
                </div>
            </div>

            <!-- Goals Summary -->
            <div class="section">
                <h2 class="section-title">
                    🎯 Goals Overview
                </h2>
                <div class="goals-summary">
                    {{goal_summary}}
                </div>
            </div>

            <!-- Business Insights -->
            <div class="section">
                <h2 class="section-title">
                    💡 Business Insights
                </h2>
                <div class="business-insights">
                    {{business_insights}}
                </div>
            </div>

            <!-- Milestone Reminders -->
            {{#if milestone_reminders}}
            <div class="section">
                <h2 class="section-title">
                    ⏰ Upcoming Milestones
                </h2>
                <div class="milestone-reminders">
                    {{{milestone_reminders}}}
                </div>
            </div>
            {{/if}}

            <!-- Sentiment Trend -->
            <div class="section">
                <h2 class="section-title">
                    📊 Your Business Mood Trend
                </h2>
                <div class="section-content">
                    {{#if personalization.recentEntryCount}}
                        Your recent journal entries show a 
                        <span class="sentiment-indicator sentiment-positive">Positive</span>
                        business mindset with a 
                        <strong>improving trend</strong> 📈
                    {{else}}
                        Start journaling to track your business mood and get personalized insights!
                    {{/if}}
                </div>
            </div>

            <!-- Call to Action -->
            <div style="text-align: center; margin-top: 40px;">
                <a href="{{baseUrl}}/dashboard" class="cta-button">
                    Open Your Dashboard
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>
                Keep building your business success with 
                <a href="{{baseUrl}}">Bizzin</a>
            </p>
            <p style="margin-top: 16px; font-size: 12px;">
                <a href="{{unsubscribeUrl}}">Unsubscribe</a> | 
                <a href="{{baseUrl}}/settings/notifications">Email Preferences</a>
            </p>
        </div>
    </div>
</body>
</html>